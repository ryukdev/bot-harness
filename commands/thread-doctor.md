---
description: Check that thread continuity is set up correctly on this machine
allowed-tools: Bash(bot-harness doctor)
---

Run `bot-harness doctor` and show its output as-is.

If anything is marked ✗ or !, say in one short line what the user should run to fix it — the
doctor output already names the command. Don't explain what you're about to do first.
